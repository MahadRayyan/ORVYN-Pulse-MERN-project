import {existsSync,readFileSync,writeFileSync} from 'node:fs';import {randomBytes} from 'node:crypto';
const target=new URL('../server/.env',import.meta.url);if(existsSync(target)){console.log('server/.env already exists; preserved without changes.');process.exit(0);}const sample=readFileSync(new URL('../server/.env.example',import.meta.url),'utf8').replace('replace_with_64_hex_characters',randomBytes(32).toString('hex'));writeFileSync(target,sample,{mode:0o600});console.log('Created server/.env with a unique encryption key. Configure MongoDB and your Meta app credentials to connect Facebook Pages and Instagram accounts.');
